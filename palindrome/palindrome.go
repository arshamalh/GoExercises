package palindrome

// Shows whether a string of characters is Palindrome or not
// Palindrome is being symmetric,
// e.g. "aba" is a palindrome because if we reverse it, it will stay the same.
func IsPalindrome(word string) bool {
	panic("Implement me!")
}
